{
  "name": "@memlume/mcp-server",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "bin": {
    "memlume-mcp": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b --stopBuildOnErrors",
    "test": "pnpm run build && vitest run",
    "typecheck": "tsc -b --stopBuildOnErrors"
  },
  "dependencies": {
    "@memlume/contracts": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "vitest": "^4.1.10"
  }
}
