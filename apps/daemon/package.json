{
  "name": "@memlume/daemon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "pnpm --filter @memlume/context-resolver build && tsc -p tsconfig.json",
    "test": "pnpm --filter @memlume/context-resolver build && vitest run",
    "typecheck": "pnpm --filter @memlume/context-resolver build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@memlume/contracts": "workspace:*",
    "@memlume/context-resolver": "workspace:*",
    "@memlume/database": "workspace:*",
    "@memlume/event-journal": "workspace:*",
    "@memlume/retrieval": "workspace:*",
    "express": "^5.2.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^24.10.1",
    "vitest": "^4.1.10"
  }
}
