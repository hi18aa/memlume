{
  "name": "@memlume/memory-compiler",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "pnpm --filter @memlume/contracts build && tsc -p tsconfig.json",
    "test": "pnpm run build && node --test",
    "typecheck": "pnpm --filter @memlume/contracts build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@memlume/contracts": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.1"
  }
}
