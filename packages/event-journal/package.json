{
  "name": "@memlume/event-journal",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm --filter @memlume/contracts build && pnpm --filter @memlume/database build && vitest run",
    "typecheck": "pnpm --filter @memlume/contracts build && pnpm --filter @memlume/database build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@memlume/contracts": "workspace:*",
    "@memlume/database": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "vitest": "^4.1.10"
  }
}
