{
  "name": "@memlume/context-resolver",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "pnpm --filter @memlume/retrieval build && tsc -p tsconfig.json",
    "test": "pnpm --filter @memlume/retrieval build && vitest run",
    "typecheck": "pnpm --filter @memlume/retrieval build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@memlume/contracts": "workspace:*",
    "@memlume/retrieval": "workspace:*"
  },
  "devDependencies": {
    "@memlume/database": "workspace:*",
    "@types/node": "^24.10.1",
    "vitest": "^4.1.10"
  }
}
